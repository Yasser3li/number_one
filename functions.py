def simple_calculator(num_one, num_two):
    add = num_one + num_two
    multiply = num_one * num_two
    sub = num_one - num_two
    div = num_one / num_two
    return add, multiply, sub, div

print(simple_calculator(10, 2))
